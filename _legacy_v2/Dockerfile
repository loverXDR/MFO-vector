# Используем легковесный официальный образ Nginx
FROM nginx:alpine

# Копируем статические файлы внутрь образа (сайт будет "зашит" в контейнер)
COPY ./src /usr/share/nginx/html

# Открываем 80 порт
EXPOSE 80

# Nginx запускается автоматически базовым образом
